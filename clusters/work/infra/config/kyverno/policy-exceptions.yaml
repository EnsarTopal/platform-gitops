# kube-system sistem pod'ları — host network zorunlu
apiVersion: kyverno.io/v2
kind: PolicyException
metadata:
  name: kube-system-exception
  namespace: kyverno
spec:
  exceptions:
  - policyName: disallow-host-namespaces
    ruleNames:
    - host-namespaces
    - autogen-host-namespaces
  - policyName: disallow-privilege-escalation
    ruleNames:
    - check-privilege-escalation
    - autogen-check-privilege-escalation
  - policyName: disallow-privileged-containers
    ruleNames:
    - check-privileged-containers
    - autogen-check-privileged-containers
  - policyName: disallow-root-user
    ruleNames:
    - check-root-user
    - autogen-check-root-user
  - policyName: require-resource-limits
    ruleNames:
    - check-resource-limits
    - autogen-check-resource-limits
  - policyName: require-image-registry
    ruleNames:
    - check-image-registry
    - autogen-check-image-registry
  - policyName: require-probes
    ruleNames:
    - check-probes
    - autogen-check-probes
  match:
    any:
    - resources:
        namespaces:
        - kube-system
        kinds:
        - Pod
        - DaemonSet
        - Deployment
        - ReplicaSet
        - StatefulSet
---
# longhorn-system — CSI plugin hostPID, driver-deployer root
apiVersion: kyverno.io/v2
kind: PolicyException
metadata:
  name: longhorn-system-exception
  namespace: kyverno
spec:
  exceptions:
  - policyName: disallow-host-namespaces
    ruleNames:
    - host-namespaces
    - autogen-host-namespaces
  - policyName: disallow-privilege-escalation
    ruleNames:
    - check-privilege-escalation
    - autogen-check-privilege-escalation
  - policyName: disallow-privileged-containers
    ruleNames:
    - check-privileged-containers
    - autogen-check-privileged-containers
  - policyName: disallow-root-user
    ruleNames:
    - check-root-user
    - autogen-check-root-user
  - policyName: require-read-only-rootfs
    ruleNames:
    - check-read-only-rootfs
    - autogen-check-read-only-rootfs
  - policyName: require-resource-limits
    ruleNames:
    - check-resource-limits
    - autogen-check-resource-limits
  - policyName: require-image-registry
    ruleNames:
    - check-image-registry
    - autogen-check-image-registry
  match:
    any:
    - resources:
        namespaces:
        - longhorn-system
        kinds:
        - Pod
        - DaemonSet
        - Deployment
        - ReplicaSet
        - StatefulSet
---
# monitoring — node-exporter hostNetwork+hostPID, promtail root
apiVersion: kyverno.io/v2
kind: PolicyException
metadata:
  name: monitoring-system-exception
  namespace: kyverno
spec:
  exceptions:
  - policyName: disallow-host-namespaces
    ruleNames:
    - host-namespaces
    - autogen-host-namespaces
  - policyName: disallow-root-user
    ruleNames:
    - check-root-user
    - autogen-check-root-user
  - policyName: require-read-only-rootfs
    ruleNames:
    - check-read-only-rootfs
    - autogen-check-read-only-rootfs
  - policyName: require-resource-limits
    ruleNames:
    - check-resource-limits
    - autogen-check-resource-limits
  match:
    any:
    - resources:
        namespaces:
        - monitoring
        kinds:
        - Pod
        - DaemonSet
        - Deployment
        - ReplicaSet
        - StatefulSet
---
# gitea — read-only rootfs uyumsuz
apiVersion: kyverno.io/v2
kind: PolicyException
metadata:
  name: gitea-exception
  namespace: kyverno
spec:
  exceptions:
  - policyName: require-read-only-rootfs
    ruleNames:
    - check-read-only-rootfs
    - autogen-check-read-only-rootfs
  - policyName: require-resource-limits
    ruleNames:
    - check-resource-limits
    - autogen-check-resource-limits
  match:
    any:
    - resources:
        namespaces:
        - gitea
        kinds:
        - Pod
        - Deployment
        - StatefulSet
        - ReplicaSet
---
# argo-rollouts — quay.io image + privilege-escalation + root user
apiVersion: kyverno.io/v2
kind: PolicyException
metadata:
  name: argo-rollouts-exception
  namespace: kyverno
spec:
  exceptions:
  - policyName: require-image-registry
    ruleNames:
    - check-image-registry
    - autogen-check-image-registry
  - policyName: require-resource-limits
    ruleNames:
    - check-resource-limits
    - autogen-check-resource-limits
  - policyName: disallow-privilege-escalation
    ruleNames:
    - check-privilege-escalation
    - autogen-check-privilege-escalation
  - policyName: disallow-root-user
    ruleNames:
    - check-root-user
    - autogen-check-root-user
  match:
    any:
    - resources:
        namespaces:
        - argo-rollouts
        kinds:
        - Pod
        - Deployment
        - ReplicaSet
---
# argocd — quay.io image
apiVersion: kyverno.io/v2
kind: PolicyException
metadata:
  name: argocd-exception
  namespace: kyverno
spec:
  exceptions:
  - policyName: require-image-registry
    ruleNames:
    - check-image-registry
    - autogen-check-image-registry
  - policyName: require-resource-limits
    ruleNames:
    - check-resource-limits
    - autogen-check-resource-limits
  match:
    any:
    - resources:
        namespaces:
        - argocd
        kinds:
        - Pod
        - Deployment
        - ReplicaSet
        - StatefulSet
---
# metallb-system — speaker hostNetwork+hostPID zorunlu
apiVersion: kyverno.io/v2
kind: PolicyException
metadata:
  name: metallb-system-exception
  namespace: kyverno
spec:
  exceptions:
  - policyName: disallow-host-namespaces
    ruleNames:
    - host-namespaces
    - autogen-host-namespaces
  - policyName: disallow-root-user
    ruleNames:
    - check-root-user
    - autogen-check-root-user
  - policyName: disallow-privilege-escalation
    ruleNames:
    - check-privilege-escalation
    - autogen-check-privilege-escalation
  - policyName: require-resource-limits
    ruleNames:
    - check-resource-limits
    - autogen-check-resource-limits
  - policyName: require-image-registry
    ruleNames:
    - check-image-registry
    - autogen-check-image-registry
  match:
    any:
    - resources:
        namespaces:
        - metallb-system
        kinds:
        - Pod
        - DaemonSet
        - Deployment
        - ReplicaSet
---
# harbor — database/registry root user, privilege-escalation
apiVersion: kyverno.io/v2
kind: PolicyException
metadata:
  name: harbor-exception
  namespace: kyverno
spec:
  exceptions:
  - policyName: disallow-root-user
    ruleNames:
    - check-root-user
    - autogen-check-root-user
  - policyName: disallow-privilege-escalation
    ruleNames:
    - check-privilege-escalation
    - autogen-check-privilege-escalation
  - policyName: require-resource-limits
    ruleNames:
    - check-resource-limits
    - autogen-check-resource-limits
  match:
    any:
    - resources:
        namespaces:
        - harbor
        kinds:
        - Pod
        - Deployment
        - ReplicaSet
        - StatefulSet
---
# external-secrets — resource limits eksik
apiVersion: kyverno.io/v2
kind: PolicyException
metadata:
  name: external-secrets-exception
  namespace: kyverno
spec:
  exceptions:
  - policyName: require-resource-limits
    ruleNames:
    - check-resource-limits
    - autogen-check-resource-limits
  match:
    any:
    - resources:
        namespaces:
        - external-secrets
        kinds:
        - Pod
        - Deployment
        - ReplicaSet
---
# kyverno — kendi controller'ları resource limits
apiVersion: kyverno.io/v2
kind: PolicyException
metadata:
  name: kyverno-system-exception
  namespace: kyverno
spec:
  exceptions:
  - policyName: require-resource-limits
    ruleNames:
    - check-resource-limits
    - autogen-check-resource-limits
  match:
    any:
    - resources:
        namespaces:
        - kyverno
        kinds:
        - Pod
        - Deployment
        - ReplicaSet
---
# ingress-nginx — resource limits
apiVersion: kyverno.io/v2
kind: PolicyException
metadata:
  name: ingress-nginx-exception
  namespace: kyverno
spec:
  exceptions:
  - policyName: require-resource-limits
    ruleNames:
    - check-resource-limits
    - autogen-check-resource-limits
  match:
    any:
    - resources:
        namespaces:
        - ingress-nginx
        kinds:
        - Pod
        - Deployment
        - ReplicaSet
---
# tekton-pipelines — probes eksik
apiVersion: kyverno.io/v2
kind: PolicyException
metadata:
  name: tekton-exception
  namespace: kyverno
spec:
  exceptions:
  - policyName: require-probes
    ruleNames:
    - check-probes
    - autogen-check-probes
  - policyName: require-resource-limits
    ruleNames:
    - check-resource-limits
    - autogen-check-resource-limits
  - policyName: require-image-registry
    ruleNames:
    - check-image-registry
    - autogen-check-image-registry
  match:
    any:
    - resources:
        namespaces:
        - tekton-pipelines
        - tekton-pipelines-resolvers
        - tekton-ci
        kinds:
        - Pod
        - Deployment
        - ReplicaSet
---
# minio — read-only rootfs uyumsuz
apiVersion: kyverno.io/v2
kind: PolicyException
metadata:
  name: minio-exception
  namespace: kyverno
spec:
  exceptions:
  - policyName: require-read-only-rootfs
    ruleNames:
    - check-read-only-rootfs
    - autogen-check-read-only-rootfs
  - policyName: require-resource-limits
    ruleNames:
    - check-resource-limits
    - autogen-check-resource-limits
  match:
    any:
    - resources:
        namespaces:
        - minio
        kinds:
        - Pod
        - Deployment
        - ReplicaSet
