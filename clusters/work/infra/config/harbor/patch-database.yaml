apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: infra-harbor-database
  namespace: harbor
spec:
  template:
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
      - name: database
        securityContext:
          runAsNonRoot: true
          allowPrivilegeEscalation: false
          capabilities:
            drop: ["ALL"]
